{
  "name": "ai-gate-iraq-official-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "node scripts/validate.mjs",
    "prepare:wrangler": "node scripts/prepare-wrangler.mjs",
    "dev": "npm run prepare:wrangler && wrangler dev --config wrangler.jsonc",
    "deploy": "npm run check && npm run prepare:wrangler && wrangler deploy --config wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
